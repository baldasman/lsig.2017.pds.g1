<div class="jumbotron">
<h2>Bem-vindo, <%= current_user.email %>! </h2>
</div>
<%= link_to " Encomendas em andamento", listar_path, :class => "btn btn-primary btn-lg margin-left glyphicon glyphicon-list pull-left" %>

  <%  @profile = current_user.profile
    if @profile.account_type == 1 %>
  <%= link_to " Criar nova encomenda", criar_path, :class => "btn btn-success btn-lg margin-left glyphicon glyphicon-plus pull-right" %>
      <% end %>

